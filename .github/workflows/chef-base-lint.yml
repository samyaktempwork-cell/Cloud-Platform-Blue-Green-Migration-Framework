name: Chef Lint (Reusable - Development Mode)

on:
  workflow_call:

jobs:
  chef_lint:
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Mock Chef Lint
        run: |
          echo "[MOCK] Chef lint skipped — development mode"
          echo "Cookstyle will run once we enable real pipelines"
